# Database
# For production (e.g., Render), add connection pool params for better performance:
# DATABASE_URL=postgresql://user:password@host:5432/dbname?connection_limit=10&pool_timeout=30&connect_timeout=10
DATABASE_URL=postgresql://user:password@localhost:5432/agentfm

# Redis Cache Configuration
# Use REDIS_URL for standard Redis connection
REDIS_URL=redis://localhost:6379

# Alternative: Use REDIS_TLS_URL for secure Redis connections (e.g., Heroku Redis)
# REDIS_TLS_URL=rediss://user:password@host:port

# Alternative: Configure Redis with separate host/port
# REDIS_HOST=localhost
# REDIS_PORT=6379

# Disable Redis caching entirely (useful for development/testing)
# REDIS_DISABLED=true

# JWT Secret
JWT_SECRET=your-secret-key-here

# Node Environment
NODE_ENV=development

# Server Port
PORT=5000

# Frontend URL (used for invite emails and OAuth redirects)
FRONTEND_URL=http://localhost:5173

# App URL (used for password reset emails, should match FRONTEND_URL in production)
APP_URL=http://localhost:5173

# AWS S3 Configuration (for persistent image and document storage in production)
# Sign up for AWS and create an S3 bucket, then create IAM credentials with S3 access
# Without these, files will be stored locally (NOT recommended for production)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_S3_BUCKET_NAME=your-bucket-name

# Optional: CloudFront CDN for faster content delivery
# Create a CloudFront distribution pointing to your S3 bucket
AWS_CLOUDFRONT_DOMAIN=your-cloudfront-domain.cloudfront.net

# Legacy Cloudinary Configuration (deprecated - migrated to S3)
# CLOUDINARY_CLOUD_NAME=your-cloud-name
# CLOUDINARY_API_KEY=your-api-key
# CLOUDINARY_API_SECRET=your-api-secret

# Blog Automation Configuration
# Enable/disable automated daily blog post generation
BLOG_AUTOMATION_ENABLED=false

# Anthropic Claude API configuration for content generation
# Get your API key from https://console.anthropic.com
ANTHROPIC_API_KEY=your-anthropic-api-key
ANTHROPIC_MODEL=claude-3-opus-20240229

# Unsplash API configuration for blog images (optional)
# Get your access key from https://unsplash.com/developers
# If not provided, placeholder images will be used
UNSPLASH_ACCESS_KEY=your-unsplash-access-key

# Blog automation settings
BLOG_INDUSTRY=facilities and property management
BLOG_TARGET_WORD_COUNT=1500
BLOG_AUTO_PUBLISH=false
BLOG_CRON_SCHEDULE=0 9 * * *
DISABLE_BLOG_AUTOMATION_CRON=false
BLOG_AUTOMATION_RUN_ON_STARTUP=false

# Stripe Payment Processing Configuration
# Get your API keys from https://dashboard.stripe.com/apikeys
# IMPORTANT: Use test keys (sk_test_...) for development, live keys (sk_live_...) for production
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Stripe Webhook Configuration
# Create webhook endpoint at https://dashboard.stripe.com/webhooks
# Set webhook URL to: https://your-domain.com/api/billing/webhook
# Required events: checkout.session.completed, customer.subscription.updated, customer.subscription.deleted, invoice.payment_failed, invoice.payment_succeeded
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Stripe Product Price IDs (from https://dashboard.stripe.com/products)
# Create recurring monthly subscription products in Stripe for each plan
STRIPE_PRICE_ID_BASIC=price_basic_plan_id
STRIPE_PRICE_ID_PROFESSIONAL=price_professional_plan_id
STRIPE_PRICE_ID_ENTERPRISE=price_enterprise_plan_id

# Optional: Stripe Add-on Price IDs for additional services
# STRIPE_ADDON_EXTRA_PROPERTIES=price_addon_properties_id
# STRIPE_ADDON_EXTRA_TEAM_MEMBERS=price_addon_team_members_id
# STRIPE_ADDON_EXTRA_STORAGE=price_addon_storage_id
# STRIPE_ADDON_EXTRA_AUTOMATION=price_addon_automation_id

AWS_REGION=us-east-1  # Or your chosen region
AWS_ACCESS_KEY_ID=your-access-key-id  # From IAM user
AWS_SECRET_ACCESS_KEY=your-secret-access-key  # From IAM user (keep secret!)
AWS_S3_BUCKET_NAME=your-bucket-name  # e.g., buildstatefm-uploads

# Optional (add after setting up CloudFront in step 5 below)
AWS_CLOUDFRONT_DOMAIN=your-cloudfront-domain.cloudfront.net